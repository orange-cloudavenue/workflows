name: "Go report card"

on:
  workflow_call:
  
permissions:
  contents: read
  
jobs:
  refresh-go-report-card:
    runs-on: [self-hosted, Linux]
    name: "Refresh"
    steps:
      - uses: creekorful/goreportcard-action@v1.0