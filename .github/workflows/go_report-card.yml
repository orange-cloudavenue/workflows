name: "Go report card"

on:
  workflow_call:
  
permissions:
  contents: read
  
jobs:
  refresh-go-report-card:
    runs-on: ubuntu-latest
    name: "Refresh"
    steps:
      - uses: creekorful/goreportcard-action@v1.0