name: "Refresh Go Proxy"

on:
  workflow_call:
  
permissions:
  contents: read
  
jobs:
  refresh:
    runs-on: ubuntu-latest
    name: "Refresh Go Proxy"
    steps:
      - uses: andrewslotin/go-proxy-pull-action@master