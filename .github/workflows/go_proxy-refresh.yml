name: "Refresh Go Proxy"

on:
  workflow_call:
  
permissions:
  contents: read
  
jobs:
  refresh:
    runs-on: ubuntu-latest
    name: "Refresh Go Proxy"
    steps:
      - uses: andrewslotin/go-proxy-pull-action@00af8a5a49c844d6dde0bbbc116b72d8fd7ae97c